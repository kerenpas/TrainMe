// data/models/EmptyResponse.kt
package com.example.trainme.data.models

/**
 * A class representing an empty response from the API.
 * Used for endpoints that don't return specific data, just success/failure status.
 */
class EmptyResponse